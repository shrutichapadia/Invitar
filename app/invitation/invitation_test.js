'use strict';

describe('myApp.invitation module', function() {

  beforeEach(module('myApp.view2'));

  describe('invitation controller', function(){

    it('should ....', inject(function($controller) {
      //spec body
      var invCtrl = $controller('invitationCtrl');
      expect(invCtrl).toBeDefined();
    }));

  });
});